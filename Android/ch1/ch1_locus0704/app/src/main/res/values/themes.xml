<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Androidab" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <!-- 스플래시 테마 -->
<!--    <style name="Theme.Splash" parent="Theme.Material3.DayNight">-->
<!--        <item name="android:windowBackground">@drawable/splash_screen</item>-->
<!--    </style>-->

    <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
        <!-- 배경색 설정 -->
        <item name="windowSplashScreenBackground">@color/gray</item>

        <!-- 스플래시 화면에 표시할 아이콘 -->
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_baseball</item>

        <!-- 스플래시가 끝나면 적용할 테마 (MainActivity의 원래 테마) -->
        <item name="postSplashScreenTheme">@style/Theme.Androidab</item>
    </style>

    <style name="Theme.Androidab" parent="Base.Theme.Androidab" />
</resources>